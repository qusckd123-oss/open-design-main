# Next Priorities - Trend Dashboard

Short by design. For rules, see `AGENT_OPERATING_RULES.md`. For live numbers, see `CURRENT_STATE.md`. Pick the highest tier with a real, live task in it - don't manufacture work in a tier just because it's next.

## P0 - Operations

- ~~Validate and commit the Coverchord Market-source config change~~ Done 2026-09-11 - typecheck/build passed, committed as `4c1c307`, pushed to `origin/feature/trend-dashboard`. See `CURRENT_STATE.md` "Market Coverchord Source Addition". Remaining optional follow-up: restart the 3001 dev server (stopped to unblock `build`) and, whenever next useful, a manual single-category `collect:market --source=COVERCHORD` run to confirm live end-to-end behavior.
- ~~Observe the first naturally scheduled Monday refresh (2026-09-14 08:30 KST) and run the post-refresh checklist~~ **Done 2026-09-14** - fired unattended, exit code 0, 8/8 sources succeeded, 0 canonical/mention duplicates, Market untouched at 667, bundle swing 89->110 (23.6%, under the 25% threshold), one expected WARN (`VISLA` zero-new-posts, its lowest-volume source) correctly not treated as a failure. Full writeup: `CURRENT_STATE.md` "P0 Scheduled Refresh Observation (2026-09-14, first natural Monday run)". This was the first healthy natural P0 observation since the scheduler was stood up.
- No other P0 item currently open. Next scheduled run: **2026-09-21 08:30 KST** - continue observing weekly the same way (don't intervene preemptively; react only to real failures).

## P1 - Quality Maintenance

- Parser/source-health fixes **only when real evidence shows a problem** (a failing source, a reproduced parsing bug, a duplicate that shouldn't exist).
- No speculative taxonomy expansion of any kind.

## P2 - Product-Planning Usability

- User explicitly authorized safe UI-only pre-gate work on 2026-09-10 while P0 remains pending. First fix completed: `/editorial` no longer shows the no-op domestic/overseas scope toggle; gender filtering remains.
- Second fix completed (2026-09-10): unified specific-item Korean label rendering (`specificItemKoreanLabel`) across `/items`, `/editorial`, and `/`'s trend×store matrix table - these previously showed the raw English SUB_ITEM enum (e.g. `TRACK JACKET`) where `/` (EditorialTrendCard) and the item detail page already showed the Korean label (`트랙 재킷`) for the exact same data. No taxonomy/dictionary expansion; reused the existing helper and the existing SUB_ITEM-only conditional pattern already established in `EditorialTrendCard`.
- New user-directed P2 focus (2026-09-11): shift toward a **visual-first trend board** that answers “요즘 뭐가 뜨는가?” using planning-ready `ITEM + DIRECT ATTRIBUTE(S) + MOOD/STYLE CONTEXT + VISUAL EVIDENCE`, not broad category counts. See `WORK_START_HERE.md`.
- Safe pre-gate work may include rendered-UI audit, visual-evidence architecture, existing-image reuse audit, and Instagram/editorial-Instagram feasibility research. Do not fabricate Instagram metrics or weaken direct-relation semantics.
- Visual-first baseline audit completed (2026-09-11): `docs/VISUAL_FIRST_TREND_BOARD_AUDIT.md` records the rendered 0-image home baseline, 587/587 article-image coverage, 0 evidence-bound bundle images, the frozen evidence-lane contract, and official-API-only Instagram feasibility boundary.
- `EditorialVisualContextStrip` completed after explicit review (2026-09-11): the current bundle now shows up to six unique existing article heroes (currently five) with publisher/date/title/link and permanent non-proof wording. Desktop and mobile rendered checks confirm first-viewport imagery and no page-level horizontal overflow. It does not replace the evidence-bound hero slot, infer mood, change ranking, or touch collectors/schema.
- Next visual-first architecture work - the healthy natural P0 observation happened 2026-09-14 (see P0 above), so that half of the gate is cleared; still waiting on a separate explicit review before implementation: preserve ordered editorial content blocks with image position so `DIRECT_BLOCK`/`ADJACENT_BLOCK` can become real, and define human-reviewed visual mood observations in a separate evidence lane. Do not infer either relationship from today's article hero images.
- Ordered-visual-evidence feasibility audit completed (2026-09-11, read-only, no code/schema/collector change): `docs/EDITORIAL_ORDERED_VISUAL_EVIDENCE_AUDIT.md` traces each of the 8 sources' parsers, confirms 0/617 stored articles carry any recoverable block/image-position data (all destroyed at parse time, none in the Prisma schema to begin with), rates per-source feasibility for a future ordered-block collector (HIGH: EYESMAG; MEDIUM: VISLA, HYPEBEAST_KR, ESQUIRE_KR, HARPERSBAZAAR_KR, COSMOPOLITAN_KR, MARIECLAIRE_KR asymmetric; LOW: NONLABEL), and proposes (design only) a minimal `OrderedContentBlock` shape plus a separate human-reviewed mood-observation shape. This still waits for explicit review and the first healthy P0 observation before any implementation - the priority tier and gate are unchanged by this audit.
- Coverchord Market-source addition approved and implemented (2026-09-11): see `CURRENT_STATE.md` "Market Coverchord Source Addition" and the P0 item above for the pending typecheck/build/commit step.
- Larger implementation that changes evidence semantics, collection architecture, ranking, taxonomy, or major IA should still wait for explicit review and/or the first healthy natural P0 scheduler observation.
- **Specific Combinations card completed and validated (2026-09-14)**: directly addresses the 2026-09-11 "too generic to plan from" feedback. `SecondaryBundleCard` -> `SpecificComboCard` (`src/components/AttributeBundle.tsx`) now shows the same attribute chips as `AttributeBundleCard` plus up to two editorial-context thumbnails per combination; the home-page section (`src/app/page.tsx`) is retitled "구체적으로 뜨는 조합" and shows 6 combinations instead of 4. No ranking/taxonomy/service change. Typecheck/test/build and rendered desktop/mobile checks passed; see `CURRENT_STATE.md` "P2 Specific Combinations Card".
- **Visual Diffusion References scaffold implemented empty and validated (2026-09-14)**: the six approved Instagram accounts now have a typed `MANUAL_CURATION` source config, and `CurrentSignalHero` has a separate permalink-only, permanently disclaimed lane for exact-item, human-approved references. The references array is intentionally empty and the lane renders nothing today; no post/permalink/item context was invented. Next action requires the user to approve an exact post/permalink before any reference object is added. Human-reviewer workflow beyond that explicit approval boundary remains undecided.
- **P0 product-audit improvement approved (2026-09-14)**: the lead `CurrentSignalHero` now adds a compact `관측된 사실 / 아직 확인되지 않음 / 기획 검토 질문` interpretation block. Facts use only existing bundle fields; unknowns vary by verified/absent attribute dimensions; the single question is explicitly human-review framing, never a recommendation. Korean-first hero naming is local to the lead signal, so the six secondary cards remain untouched. Typecheck/test/build, `git diff --check`, and rendered desktop/mobile checks passed; see `CURRENT_STATE.md` "P0 Product UX - Lead Signal Interpretation". Shared Korean display-label consistency remains a separate cleanup task.
- **P0 coverage vs recent direction approved (2026-09-14)**: the home dashboard now separates cumulative `관측 강도`, comparable-window `최근 방향`, and `최신 관측`. Unsupported `뜨는` headings were replaced; exact bundles and UNI/WOMEN-filtered rows honestly show direction `판단 불가` because no valid comparable-window metric exists at those scopes. Related rows keep their existing cumulative article-presence ordering and show the underlying recent/previous seven-day article counts beside `증가 / 유지 / 감소`. No ranking, collector, schema, DB, Market, demand, or commercial-validation change. See `CURRENT_STATE.md` "P0 Product UX - Coverage vs Recent Direction".
- **P1 Watchlist merge implemented and validated (2026-09-14)**: supersedes the separate hero (formerly `CurrentSignalHero`) and the six-card `구체적으로 관측된 조합` block from the two bullets above - both are now one coherent 5-signal "상품기획 워치리스트" (`SelectedSignalDetail` + new `WatchlistRow` in `src/components/AttributeBundle.tsx`, plus a new small client selection shell `src/components/Watchlist.tsx`). Same frozen bundle ordering - the default-selected signal is still the existing `repeatedBundle` gate (`independentEvidenceClusterCount >= 2`), unchanged. Desktop shows a compact row list and the selected-signal detail together in one ~44%/56% layout without scrolling past a giant hero first; mobile is a single-open vertical accordion. Article thumbnails remain editorial context only (a persistent visible "기사 이미지" label, never a hover-only tooltip, never direct proof); exact-bundle 최근 방향 remains unavailable and is stated once at the section/detail level, never repeated per row; a persistent "관측 근거 기준 정렬 · 성장·판매 순위 아님" line disambiguates row position from any commercial rank. Same pass also unified compact-row/detail naming (both now reuse `buildSignalInterpretation(bundle).signalName`) so a row never shows a different name than its own open detail (e.g. the old "스트라이프 SHIRT" row next to a "스트라이프 셔츠" detail heading). No ranking/service/taxonomy/schema/collector/DB change. Typecheck/test/build/`git diff --check` and rendered 1440x1000/390x844 checks passed; see `CURRENT_STATE.md` "P1 Product UX - Unified Watchlist".
- Trend research source-map audit completed (2026-09-11, docs/config-independent, no code/config/collector change): `docs/TREND_RESEARCH_SOURCE_REGISTRY.md` checks 8 sources the user actually references (5 Instagram accounts/posts, HBX women's, Musinsa's content/magazine section, Coverchord) against existing repo coverage, classifies each into an EDITORIAL DIRECT EVIDENCE / VISUAL DIFFUSION-STYLE CONTEXT / MARKET-ASSORTMENT / MANUAL-ONLY lane, and proposes (design only) a `VisualDiffusionSourceConfig`/`VisualDiffusionReference` model kept structurally separate from `EditorialMention`/ranking. Two concrete new-action candidates surfaced, neither implemented in this pass: (1) `coverchord.com` is a Shopify storefront with a confirmed public `products.json` endpoint - a small, isolated, config-only Market-source addition mirroring Slam Jam/Stussy, ready whenever explicitly approved and independent of the Editorial P0 gate; (2) ~~the 4 editorial Instagram accounts plus the 1 example permalink are candidates for a `MANUAL_CURATION`-tier registry entry, pending explicit approval~~ **Approved and registered 2026-09-14** - see `docs/TREND_RESEARCH_SOURCE_REGISTRY.md` Section 11 and `CURRENT_STATE.md`. 6 accounts total (the original 4 plus 2 more found and bio-verified in the same pass) registered docs-only in the Markdown registry; no code/collector was added since `VisualDiffusionSourceConfig` itself remains an unimplemented design proposal - no automated Instagram collection exists or is proposed. Human reviewer workflow still undecided.

## P3 - Optional Future Research

- A new publisher family: only when source diversity becomes a *demonstrated* bottleneck again (it currently isn't - see `docs/EDITORIAL_SIGNAL_SATURATION_AUDIT.md` Section 20).
- New taxonomy vocabulary: only when the current corpus proves a recoverable trust-tier signal exists to capture (not a speculative "might help" addition).
- ~~New Market source: `COVERCHORD`~~ Approved and added 2026-09-11 - see P0's validation/commit item and `CURRENT_STATE.md`.

## Explicitly NOT Current Priority

Do not pursue these unless the user explicitly reopens them in-session:

- More generic source collection.
- BAGS taxonomy, ACCESSORIES taxonomy, COAT taxonomy (tried, reverted - see `docs/EDITORIAL_CATEGORY_COVERAGE_GAP_AUDIT.md`).
- Product Reference (closed/frozen).
- Ranking redesign (current sort order is documented, working, and frozen - see `CURRENT_STATE.md` "Current Ranking Order").
- Scheduler redesign (current Windows Task Scheduler setup is working - verify, don't rebuild).
